app: Coot

ingredients:
  packages:
    - coot
    - coot-data
    - libcootapi1.1
  dist: unstable
  sources:
    - deb http://deb.debian.org/debian unstable main contrib non-free

script:
  - sed -i 's|^Exec=.*|Exec=env GDK_DEBUG=gl-glx coot|' coot.desktop
